{
  "name": "avid-transcript-importer",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "test": "node --test tests/*.test.cjs && python3 -m unittest discover -s tests -p \"test_*.py\"",
    "build:demo": "node scripts/build-demo.cjs",
    "demo": "npm run build:demo && python3 -m http.server 8790 --bind 0.0.0.0 --directory demo",
    "test:browser": "playwright test"
  },
  "engines": {
    "node": ">=22"
  },
  "description": "Format Avid transcript text exports as client-review documents in Google Docs.",
  "dependencies": {
    "pyodide": "314.0.7"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
